# Session 012 — Centered phase orbs

**Status:** done
**When:** 2026-09-04
**Branch:** `feature`
**Queue:** 001 → 002 → 003 → 004 → 005 → 006 → 007 → 008 → 009 → 010 → 011 → this file.

## Ask

Center the thinking orb above the chat composer and show the orb/label associated with each learning phase. For the diagnostic smoke-test phase, reuse the agent-planning visual but label it “smoke testing.”

## Shipped

- Centered `.kaiako-orb-pill` in the prompt bar.
- Added phase-aware orb presentations for goal setting, diagnostic smoke testing, and teaching.
- Renamed the generic shaping tool label to “agent planning.”
- Added label overrides so the diagnostic phase can use the agent-planning visual with the “smoke testing” label.
- Restored the phase orb after completed or failed turns instead of hiding it, while preserving transient tool-state animations during active Pi responses.

## Verification

- `npm run build` passed in `kaiako/`.
- Existing unrelated worktree changes were preserved; no commit was created.

## Files

- `kaiako/src/thinking-orb.ts` — phase presentations, label overrides, and agent-planning label.
- `kaiako/src/prompt-bar.ts` — phase-aware orb handle.
- `kaiako/src/kaiako-view.ts` — phase synchronization at render, message hydration, MCQ, completion, and error states.
- `kaiako/styles.css` — centered orb layout.
- `kaiako/main.js` — rebuilt plugin bundle.
