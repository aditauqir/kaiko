# Session 004 — Pi RPC wrap + lock-in contrast + composer

**Status:** done  
**When:** 2026-09-03  
**Branch:** `feature`  
**Queue:** 001 → 002 → 003 → this file.

## Pi

- `PiHost` spawns `pi --mode rpc` with `PI_CODING_AGENT_DIR={dataFolder}/pi`, session dir under that, vault as cwd
- API keys passed as env (`ANTHROPIC_API_KEY`, etc.)
- Send → RPC `prompt`; assistant text appended to the session markdown
- Auto-start / netsearch toggles actually sync (`pi-web-access` install best-effort)
- Globe highlights on search/web tool calls; sigma highlights on math/formula tools or `$` in stream

## UI

- Lock-in (dark): text/icons forced toward white for contrast
- Composer: globe (inert except highlight), model picker, formula (inert except highlight), circular send that follows Obsidian light/dark
- Not React/glimm (Obsidian plugin); layout matches the PromptBar controls
- Settings modal uses Obsidian `mod-settings` chrome: left nav + search, native Setting rows
